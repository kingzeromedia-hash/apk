import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schoolresultshub.app',
  appName: 'School Results Hub',
  webDir: 'dist'
};

export default config;
